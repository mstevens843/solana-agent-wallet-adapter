{
  "$schema": "./connector-pack.schema.json",
  "id": "jupiter",
  "name": "Jupiter",
  "status": "first_class",
  "aliases": ["jupiter", "jup", "jupiter swap", "jupiter swap api v2", "jupiter ultra", "jupiter lend"],
  "supportedClusters": ["mainnet-beta"],
  "requiredConfig": [
    {
      "name": "JUPITER_API_KEY or JUP_API_KEY",
      "scope": "env",
      "required": true,
      "description": "Required for Jupiter Swap API v2 order previews, prepared swaps, and swap execution."
    },
    {
      "name": "Dialect client key",
      "scope": "dialect_client_key",
      "required": false,
      "description": "Not used by MCP Jupiter Swap API v2 support. Jupiter Lend remains planned."
    }
  ],
  "readCapabilities": [
    {
      "toolName": "solana_jupiter_order_preview",
      "source": "jupiter-swap-v2",
      "implemented": true,
      "requiresClientKey": false,
      "returns": "Read-only Jupiter Swap API v2 order preview with output amount, minimum output threshold, slippage, price impact, route plan, router/mode metadata, fee fields, and request id.",
      "facts": ["inputMint", "outputMint", "inAmount", "outAmount", "otherAmountThreshold", "slippageBps", "priceImpact", "routePlan", "router", "mode", "feeBps", "feeMint", "requestId", "hasTransaction"]
    },
    {
      "toolName": "solana_get_swap_quote",
      "source": "jupiter-swap-v2",
      "implemented": true,
      "requiresClientKey": false,
      "returns": "Compatibility alias for Jupiter order preview. Returns the same normalized connector facts.",
      "facts": ["outAmount", "slippageBps", "priceImpact"]
    },
    {
      "toolName": "dialect_positions",
      "source": "dialect-markets",
      "implemented": false,
      "requiresClientKey": true,
      "returns": "Planned Jupiter Lend positions. Not implemented in this Swap API v2 pass.",
      "facts": ["positionBalance", "borrowBalance", "collateralToken", "healthFactor"]
    },
    {
      "toolName": "dialect_markets",
      "source": "dialect-markets",
      "implemented": false,
      "requiresClientKey": true,
      "returns": "Planned Jupiter Lend market facts. Not implemented in this Swap API v2 pass.",
      "facts": ["marketApy", "borrowApy", "liquidity", "actionUrl"]
    }
  ],
  "writeCapabilities": [
    {
      "actionKind": "swap",
      "executionMode": "first_class_adapter",
      "implemented": true,
      "prepares": "A manual-approval PreparedAction for a capped Jupiter swap. Quote and transaction are refreshed when the user sends it to the wallet.",
      "executesVia": "solana_execute_prepared_action",
      "userApproves": "A Jupiter-built swap transaction for the exact input token, output token, amount, and slippage cap."
    },
    {
      "actionKind": "blink_action",
      "executionMode": "blink",
      "implemented": false,
      "prepares": "Planned Jupiter Lend, Trigger, or Recurring action review. MCP runtime does not expose those Jupiter product surfaces in this pass.",
      "executesVia": "solana_execute_prepared_action",
      "userApproves": "A future Jupiter product transaction after explicit wallet approval. Not implemented in this pass."
    }
  ],
  "requiredUserInputs": [
    {
      "name": "amount",
      "kind": "number",
      "required": true,
      "appliesTo": ["swap"],
      "prompt": "How much do you want to swap?"
    },
    {
      "name": "inputToken",
      "kind": "text",
      "required": true,
      "appliesTo": ["swap"],
      "prompt": "Which token are you swapping from?"
    },
    {
      "name": "outputToken",
      "kind": "text",
      "required": true,
      "appliesTo": ["swap"],
      "prompt": "Which token do you want to receive?"
    },
    {
      "name": "slippageBps",
      "kind": "number",
      "required": false,
      "appliesTo": ["swap"],
      "prompt": "What max slippage should I use in basis points? Leave blank to use the configured cap."
    },
    {
      "name": "blinkUrl",
      "kind": "text",
      "required": true,
      "appliesTo": ["blink_action"],
      "prompt": "Jupiter non-swap product support is not implemented in this runtime yet."
    }
  ],
  "requiredConnectorFacts": [
    "Jupiter Swap API v2 order preview before review",
    "Output amount and minimum output threshold",
    "Router, mode, route plan, and fee fields when reported",
    "Slippage cap in basis points",
    "Price impact when reported",
    "Token mint and decimals for any mint not in the allowlist",
    "Connected wallet address for wallet-specific order previews"
  ],
  "unsupportedRequests": [
    "Swap without an amount, input token, or output token.",
    "Use a slippage cap above the configured wallet limit.",
    "Guarantee an exact output after the quote is shown. Jupiter routes can change before signing.",
    "Lend, borrow, withdraw, repay, Trigger, Recurring, Prediction, or Perps through Jupiter in this Swap API v2 pass.",
    "Operate on any cluster other than mainnet-beta.",
    "Claim that Jupiter or the agent can sign or submit without the user's wallet."
  ],
  "actionPreparationNotes": "Normalize SOL to wrapped SOL for Jupiter Swap API v2, resolve allowlisted symbols or mint decimals, enforce maxSwapInput and maxSlippageBps, then prepare a swap inbox item with refreshAtExecution=true. For previews, call Jupiter /order with taker, inputMint, outputMint, amount, and slippageBps. Do not store raw transaction bytes in prepared actions or expose them in findings.",
  "riskNotes": "Quotes can expire or change before approval. Price impact, minimum output, and slippage must be shown. Unknown token mints should be displayed as short mints unless metadata is available.",
  "approvalBoundary": {
    "readGated": true,
    "writeGated": true,
    "blinkBacked": false,
    "summary": "safety.prepare_only"
  },
  "walletApprovalBoundary": "Jupiter previews are read-only. Prepared swaps stay in the approval inbox until the user sends them to the wallet. The wallet signs and broadcasts; the agent does not.",
  "examples": {
    "requests": [
      {
        "user": "swap 0.25 SOL to USDC",
        "classification": "prepare_approval_action",
        "writeAction": "swap",
        "agentResponse": "Prepared a Jupiter swap from SOL to USDC for 0.25 SOL with the configured slippage cap. This prepares a wallet approval request; it does not sign."
      },
      {
        "user": "quote 1 SOL to JUP before I sign",
        "classification": "read_only_answer",
        "readTool": "solana_jupiter_order_preview",
        "agentResponse": "Read a Jupiter Swap API v2 order preview and returned expected output, minimum output, slippage, route, fee, and price impact facts. The connector can read facts, but the wallet still controls approval."
      },
      {
        "user": "swap SOL to USDC",
        "classification": "needs_input",
        "needsInputQuestions": ["How much SOL do you want to swap?"],
        "agentResponse": "The agent is missing facts needed to decide. Tell me the amount before I prepare the approval request."
      },
      {
        "user": "swap 1000 SOL to USDC",
        "classification": "unsupported",
        "denialReason": "safety.denied_with_reason",
        "agentResponse": "The agent denied this because the amount exceeds the configured max swap input. Lower the amount or update the policy."
      },
      {
        "user": "swap 0.1 SOL to USDC with 10% slippage",
        "classification": "unsupported",
        "denialReason": "safety.denied_with_reason",
        "agentResponse": "The agent denied this because 10% slippage exceeds the configured wallet cap."
      },
      {
        "user": "swap 0.1 SOL to whatever gives the best yield",
        "classification": "needs_input",
        "needsInputQuestions": ["Which output token should I use?"],
        "agentResponse": "The agent is missing facts needed to decide. Choose an output token before a swap can be reviewed."
      },
      {
        "user": "lend 5 USDC on Jupiter",
        "classification": "unsupported",
        "denialReason": "safety.action_not_exposed",
        "agentResponse": "The agent cannot prepare Jupiter Lend yet because this pass only implements Jupiter Swap API v2."
      },
      {
        "user": "repay my Jupiter loan automatically every week",
        "classification": "unsupported",
        "denialReason": "safety.no_autopay_claim",
        "agentResponse": "The agent denied this because Jupiter Lend and Jupiter-native recurring automation are not first-class in MCP runtime yet."
      },
      {
        "user": "borrow on Jupiter without opening my wallet",
        "classification": "unsupported",
        "denialReason": "safety.no_autonomous_funds",
        "agentResponse": "The agent denied this because the connector cannot move funds without wallet approval."
      },
      {
        "user": "is this Jupiter swap safe?",
        "classification": "proof_only_review",
        "readTool": "solana_jupiter_order_preview",
        "agentResponse": "Reviewed the quote facts and warnings, but did not guarantee safety. The user still decides whether to approve in the wallet."
      },
      {
        "user": "swap 0.5 SOL to this mint 7GCihgDB8fe6KNjn2MYtkzzcRjQy3t9GHdC8uHYmW2hr",
        "classification": "prepare_approval_action",
        "writeAction": "swap",
        "agentResponse": "Resolved the output token as a mint address, requested a Jupiter preview, and prepared a wallet-approval swap if the mint decimals and policy checks pass."
      },
      {
        "user": "swap on devnet through Jupiter",
        "classification": "unsupported",
        "denialReason": "safety.action_not_exposed",
        "agentResponse": "The agent denied this because Jupiter Swap API v2 swaps are only supported on mainnet-beta in this runtime."
      }
    ]
  },
  "mirrorsCanonical": {
    "file": "apps/browser-demo/src/connectedDapps.ts",
    "entry": "jupiter",
    "note": "Browser catalog lists Jupiter as first-class for Swap API v2. Parent-roadmap product surfaces remain planned until their child plans land."
  }
}
