{
  "name": "@solana-agent-wallet-adapter/embedded-wallet",
  "version": "0.0.1",
  "description": "Software wallet built into the Agentic desktop shell. Exposes the native Tauri keypair via the Solana Wallet Standard so the existing picker discovers it automatically.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "src",
    "README.md"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@solana/wallet-standard-features": "^1.3.0",
    "@solana/web3.js": "^1.95.0",
    "@wallet-standard/app": "^1.1.1",
    "@wallet-standard/base": "^1.1.1",
    "@wallet-standard/features": "^1.1.1",
    "bs58": "^6.0.0"
  },
  "devDependencies": {
    "@noble/curves": "^1.4.0",
    "typescript": "^5.6.0",
    "vitest": "^4.1.9"
  }
}
